master:
    name: "BrewMaster"
    ip: "localhost"
    port: "5672"

workers:
    - worker:
        name: "Mash Dude"
        ip: "localhost"
        port: "5672"
        class: "MashWorker"
        outputs:
            - output:
                name: "Mash Tun"
                device: "SSR"
                io: "gpio0"
        inputs:
            - input:
                name: "Temperature"
                device: "Probe"
                io: "gpio1"
